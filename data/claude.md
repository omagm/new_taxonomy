This directory contains JSON data files with machine taxonomies and models. These files are used by the MCP server to manage and serve the taxonomy data. They also serve as the data source for the web app inside ```../interface``` which renders this data in a human friendly way. 

The data is structured according to the schema definitions found in the ```/schemas``` directory.