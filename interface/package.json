{
  "name": "interface",
  "version": "1.0.0",
  "description": "A web-based visualization tool for inspecting the taxonomy data stored in `../data/`.",
  "main": "inspector.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "lsof -ti:3000 | xargs kill -9 2>/dev/null || true && node server-watch.js",
    "predev": "lsof -ti:3000 | xargs kill -9 2>/dev/null || true"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chokidar": "^4.0.3"
  }
}
